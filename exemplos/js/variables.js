// A unica diferença são os scopos
// Var está visível para as blocos próximos
// Let está visivel somente para o bloco atual
var name = "Michel";
let age = 22;

// String
var string = "String";

//number
var number = 30;

//Double
var double = 3.0;

// Array
var array = [ 0, "nome", [ "Outro array" ], { 'objeto' : "objeto" } ];

// Object
var obj = { 'nomeAtributo' : "Valor Atributo" };

//Functions
// diferença entre os 2 metodos:
// A primeira, pode ser invocada antes da "criação"
// A segunda segue uma linha de reciocinio estrutural e só pode ser invocada após sua "criação"
function exemploFunction(){

}

var exemploOutraFunction = function(){

}

//-------//
